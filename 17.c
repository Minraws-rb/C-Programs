#include<stdio.h>
int main()
{
    printf("This to check different escape squence:\n ");
    printf("just now a new line character was used for new line");
    printf(" \bJust now backspace character was used to back the space at the first line of this statement");
    printf("horizontal tab is used in between a\tb to give a tab");
    return 0;
}